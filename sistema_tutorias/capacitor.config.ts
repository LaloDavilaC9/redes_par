import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'sistema_tutorias',
  webDir: 'dist/sistema_tutorias',
  bundledWebRuntime: false
};

export default config;
